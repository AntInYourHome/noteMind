# NoteMind 使用教程

## 一、它是什么？

NoteMind 是一个**把各种文件转成 Obsidian 笔记**的工具。

你丢给它 PDF、Word、PPT、Excel、图片、OneNote、Markdown、文本文件，它会自动：

1. 提取文件内容
2. AI 生成摘要和标签
3. 自动分类到对应文件夹
4. 生成标准 Markdown 笔记
5. 去重防呆（不重复处理）
6. 断点续传（中断后可恢复）
7. 批量处理成千上万个文件
8. 健康监控和指标报告

最终你在 Obsidian 里就能看到整理好的知识树。

---

## 二、安装（3 步）

### 前提条件

- Python 3.10+（`python3 --version` 检查）
- Linux / macOS / Windows 均可

### 第 1 步：解压

```bash
tar -xzf noteMind_20260429.tar.gz
cd noteMind
```

### 第 2 步：编辑配置

用任意编辑器打开 `config.json`，修改两项：

```json
{
    "ai": {
        "api_key": "你的API密钥",
        "model": "qwen3.6-flash"
    },
    "vault": {
        "path": "~/knowledge-base",
        "categories": ["芯片", "安全", "项目", "笔记", "其他"]
    }
}
```

**需要改的：**
- `api_key`：改成你自己的 API key（阿里云百炼平台申请）
- `vault.path`：改成你想存放笔记的目录
- `categories`：改成你自己的分类

Windows 用户可以用记事本：
```
notepad config.json
```

### 第 3 步：安装可选依赖（按需）

核心功能（图片识别、文本处理）**不需要任何依赖**，直接运行即可。

如果你需要解析 PDF/Word/PPT/Excel，安装对应的库：

```bash
pip install pymupdf        # PDF
pip install python-docx    # Word
pip install python-pptx    # PPT
pip install openpyxl       # Excel
```

或者运行安装脚本（交互式选择）：
```bash
./install.sh
```

---

## 三、使用

### 基本用法

```bash
# 把文件放到一个目录
mkdir ~/my-files
cp ~/Downloads/*.pdf ~/my-files/
cp ~/Desktop/*.jpg ~/my-files/

# 一键导入
python import.py --source ~/my-files
```

### 查看结果

```bash
# 查看生成的笔记
ls ~/knowledge-base/芯片/
cat ~/knowledge-base/MOC.md

# 用 Obsidian 打开 ~/knowledge-base 目录
```

### 常用参数

| 参数 | 说明 | 示例 |
|------|------|------|
| `--source` | **必需**，源文件目录 | `--source ~/downloads` |
| `--vault` | Vault 目录（覆盖 config） | `--vault ~/my-vault` |
| `--config` | 配置文件路径 | `--config /path/to/config.json` |
| `--dry-run` | 预览模式，不实际写入 | `--dry-run` |
| `--resume` | 从上次中断的检查点恢复 | `--resume` |

### 示例

```bash
# 预览模式（先看有多少文件）
python import.py --source ~/downloads --dry-run

# 指定不同的 Vault
python import.py --source ~/work --vault ~/work-knowledge

# 指定配置文件
python import.py --source ~/docs --config /etc/notemind.json

# 从断点恢复（处理大批量文件中断后继续）
python import.py --source ~/massive-collection --resume
```

---

## 四、处理流程

```
你放入文件
    │
    ▼
┌─────────────────────┐
│ 1. 收集文件           │  扫描目录下所有支持的文件
│ 2. MD5 去重          │  已处理过的自动跳过
│ 3. 解析内容          │  提取文本 / 标记为图片
│ 4. AI 分析           │  摘要 + 标签 + 自动分类
│ 5. 归档原始文件      │  复制到 _archive/
│ 6. 写入 Markdown     │  生成笔记到对应分类文件夹
│ 7. 更新知识树        │  更新 MOC.md 总览页
│ 8. 输出报告          │  成功/失败统计
└─────────────────────┘
    │
    ▼
Obsidian 里查看笔记
```

---

## 五、Obsidian 联动

### 打开 Vault

1. 打开 Obsidian
2. 点击 **"打开另一个 Vault"** 或 **"打开文件夹"**
3. 选择你的 Vault 目录（默认 `~/knowledge-base`）
4. 完成！

### 你能看到什么

- **MOC.md** — 知识树总览，列出所有分类和笔记
- **分类文件夹** — 芯片、安全、项目等，每个笔记在里面
- **全文搜索** — 按 `Ctrl+Shift+F` 搜索所有内容
- **标签搜索** — 搜索 `#芯片` 看所有带此标签的笔记
- **双向链接** — 点击 `[[笔记名]]` 跳转到另一篇笔记

### 推荐插件（可选）

在 Obsidian 设置 → 社区插件中安装：

| 插件 | 用途 |
|------|------|
| **Waypoint** | 自动生成文件夹目录 |
| **Dataview** | 查询和展示笔记元数据 |
| **Calendar** | 按日期浏览笔记 |

---

## 六、文件都去哪了？

```
原始文件 → ~/knowledge-base/_archive/  （归档备份）
生成的笔记 → ~/knowledge-base/芯片/2026-04-29-xxx.md
失败的文件 → ~/knowledge-base/_failed/  （含错误日志）
```

你的原始文件不会被删除，只是复制了一份到 archive。

---

## 七、Windows 使用

### 方法 1：PowerShell

```powershell
cd C:\Users\yourname\noteMind
python import.py --source C:\Users\yourname\Desktop\inbox
```

### 方法 2：双击运行

创建一个 `导入.bat` 放在桌面：

```batch
@echo off
cd /d "C:\Users\yourname\noteMind"
python import.py --source "%USERPROFILE%\Desktop\inbox"
pause
```

双击即可运行。

### 方法 3：WSL

```bash
wsl
cd /mnt/c/Users/yourname/noteMind
python import.py --source /mnt/c/Users/yourname/Desktop/inbox
```

---

## 八、常见问题

### Q: 报 "QWEN_API_KEY 未设置"

在 `config.json` 中确保 `ai.api_key` 有值：
```json
"ai": {
    "api_key": "sk-你的key"
}
```

### Q: 某个文件处理失败

1. 检查是否安装了对应的解析库（如 PDF 需要 pymupdf）
2. 查看 `_failed/文件名.error.txt` 中的错误信息
3. 修复后重新运行，已成功的文件会被 MD5 去重跳过

### Q: 想重新导入所有文件

删除 `Vault/.notemind_index.json`，然后重新运行。

### Q: 处理中断了怎么办？

使用 `--resume` 参数重新启动，程序会自动从检查点恢复，不会重复处理已完成的文件。
检查点保存在 `logs/checkpoint.json`。

### Q: API 调用太慢

网络问题。可以在 `config.json` 中调整超时或选择更快的模型。

### Q: 支持多少种文件格式？

| 格式 | 状态 | 依赖 |
|------|------|------|
| .pdf | ✅ | pymupdf |
| .docx/.doc | ✅ | python-docx |
| .pptx/.ppt | ✅ | python-pptx |
| .xlsx/.xls | ✅ | openpyxl |
| .md/.markdown | ✅ | 无 |
| .txt/.log/.csv | ✅ | 无 |
| .jpg/.png/.gif/.bmp/.webp | ✅ AI 识别 | 无 |
| .one (OneNote) | ✅ | onenote2xml |

---

## 九、配置文件完整说明

```json
{
    // AI 配置
    "ai": {
        "api_key": "sk-xxx",           // 你的 API key
        "model": "qwen3.6-flash",      // AI 模型名称
        "base_url": "https://dashscope.aliyuncs.com/compatible-mode/v1"  // API 地址
    },

    // Vault 配置
    "vault": {
        "path": "~/knowledge-base",    // Vault 目录（支持 ~ 扩展）
        "categories": [                // 预设分类（AI 会自动归入这些目录）
            "芯片", "安全", "项目", "笔记", "其他"
        ]
    },

    // 导入行为
    "import": {
        "archive_dir": "_archive",     // 原始文件归档目录
        "failed_dir": "_failed",       // 失败文件目录
        "max_retries": 3,              // 解析失败最大重试次数
        "retry_delay": 2,              // 重试间隔（秒）
        "dedup": true,                 // 是否启用 MD5 去重
        "dedup_index": ".notemind_index.json"  // 去重索引文件名
    },

    // 日志
    "logging": {
        "level": "INFO",               // DEBUG / INFO / WARNING / ERROR
        "file": "logs/import.log",     // 日志文件路径
        "log_dir": "logs"             // 日志目录（含检查点和报告）
    }
}
```

---

## 十、测试验证

项目包含自动化测试，运行冒烟测试：

```bash
python tests/test_smoke.py
```

完整的测试计划见 `TEST_PLAN.md`。
