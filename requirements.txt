# NoteMind — 零外部依赖（纯 Python 标准库）
# 所有核心功能仅需 Python 3.10+

# 可选依赖：按需安装
# pip install pymupdf         # PDF 解析
# pip install python-docx     # Word 解析
# pip install python-pptx     # PPT 解析
# pip install openpyxl        # Excel 解析
# pip install mistune         # Markdown 格式清洗
# pip install onenote2xml     # OneNote 解析
# pip install rapidocr_onnxruntime  # 本地 OCR（图片文字提取）

# --- VLM 本地图片描述（MiniMind-V） ---
# 启用本地 AI 图片描述，~3s/张，CPU 可用，无需联网
# 先安装模型: python scripts/setup_vlm.py
# pip install torch --index-url https://download.pytorch.org/whl/cpu  # Linux CPU
# pip install transformers>=4.40  # 模型加载
# pip install Pillow               # 图像处理
