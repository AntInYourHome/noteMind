# NoteMind — Dependencies
# Core functionality requires NO external packages (Python 3.10+ stdlib only).
# Install optional groups as needed:

# Format parsers:
#   pip install "notemind[pdf]"        # PDF support
#   pip install "notemind[office]"     # Word, PPT, Excel support
#   pip install "notemind[all]"        # All format parsers

# Local VLM (AI image description, CPU-only):
#   pip install "notemind[vlm]"

# ─── Format Support ───
# PDF
pymupdf

# Office documents
python-docx
python-pptx
openpyxl

# Markdown cleaning
mistune

# OneNote
onenote2xml
lxml

# Local OCR (fallback when document text is insufficient)
rapidocr_onnxruntime

# ─── Local VLM (MiniMind-V) — Optional ───
# Model weights (~277MB) must be downloaded separately:
#   notemind vlm-install        (after pip install notemind[vlm])
# torch>=2.0
# transformers>=4.40
# Pillow
